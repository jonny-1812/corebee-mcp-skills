#!/bin/bash
# remind-resolution-note.sh — PostToolUse hook for close_conversation
# Reminds the user to add a resolution note after closing a conversation

echo "REMINDER: Consider adding a resolution note to the contact record for future reference."
echo "Use: add_contact_note with a summary of how the issue was resolved."
exit 0
